services:
  - type: web
    name: parkquick-sg
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: LTA_API_KEY
        sync: false
      - key: PORT
        value: 3000
